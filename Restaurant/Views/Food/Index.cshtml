@*@model IEnumerable<Restaurant.Models.FoodViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<input type="button" style="margin-top:15px;margin-bottom:15px" class="btn btn-default" value="Add new item" onclick="location.href='@Url.Action("Create")'"/>

<table id="myCustomTable" >
    <tr >
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Ingredients)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ingredients)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuantityAsString)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PriceAsString)
            </td>
            <td>
                <input type="button" style="margin-top:5px;margin-bottom:5px" class="btn btn-default" value="Edit" onclick="location.href='@Url.Action("Edit", new { id = item.ID })'" />
                <input type="button" style="margin-top:5px;margin-bottom:5px" class="btn btn-default" value="Details" onclick="location.href='@Url.Action("Details", new { id = item.ID })'" />
                <input type="button" style="margin-top:5px;margin-bottom:5px" class="btn btn-default" value="Delete" onclick="location.href='@Url.Action("Delete", new { id = item.ID })'" />
            </td>
        </tr>
    }
</table>
<div class="grid-container">
    <img class="galery" src="@Url.Action("GetImage", "Image", new { id = 1 })" />
    <img class="galery " src="@Url.Action("GetImage", "Image", new { id = 2 })" />
    <img class="galery" src="@Url.Action("GetImage", "Image", new { id = 3 })" />
</div>*@